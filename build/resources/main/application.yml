spring:
  datasource:
    url: jdbc:mysql://localhost:3306/VitualThread
    username: root
    password: Janghojin00@
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect

  threads:
    virtual:
      enabled: true